# Handoff Snapshot

Created: 2026-08-20 05:46
Agent / Tool: Claude Code (Fable 5) + QA 서브에이전트(Playwright, 6라운드)
Task: qa-loop-r2-r9 — 첫 방문 UX · 인터랙티브 튜토리얼 · QA 루프
Risk Level: L2 (새 UI 모듈 `src/ui/tour/`, 커맨드 의미 변경)
Project Version: 0.1.0

## Session Goal

사용자 지시: ① 첫 방문자 관점으로 막힘 없이 쓰이는지 판단·수정 ② 페이지 검증 에이전트를 두고 수정 루프 ③ 쿠키 기반 첫 방문 감지 + 화면 요소를 동적으로 하이라이트하는 참여형 튜토리얼.

## Completed

- R2 첫 방문 UX: 런처(양 팀 채우고 시작/예시), 체크리스트, fling 오인 버그, 첫 채움 공 보유자, 자동 대응 기본 팀 — CHG-010
- R3 인터랙티브 투어 `src/ui/tour/` (8단계 스포트라이트, 행동 시 자동 진행, 쿠키+localStorage) — CHG-011
- R4~R9 QA 루프 6라운드 수정 — CHG-012~017. 대표: 기본 팀 history 밖 · 문서 교체 undo · 패스 끝 드래그 수신자 재해석 · Space playFrom/모달리티 · 체인 패스 순환(passerFor) · **PitchStage stale closure** · 보유 공 드래그 · 포메이션 변경 고스트 패스 정리 · 트랙 패널 높이 · 패스 삭제 고아 possessed.
- QA 하네스: scratchpad `pw/lib.cjs`, `baseline.cjs`, `tour.cjs`, `chain.cjs`, `stale.cjs`, `r6.cjs`, `r7.cjs` (프로젝트 의존성 아님; 메모리 `qa-playwright-harness` 참조).

## Files Touched

| File | Change | Change ID |
|---|---|---|
| src/ui/EmptyState.tsx, GettingStarted.tsx | 신규 | CHG-010 |
| src/ui/tour/* | 신규 (tourSteps/TourOverlay/tourStorage/test) | CHG-011 |
| src/ui/pitch/PitchStage.tsx, fling.ts | fling/stale closure/ball drag/ballAfter/ghost | CHG-010,015,016 |
| src/editor/segmentCommands.ts | syncTravelReceiver, passerFor/possessTrigger, tailEndAt, giveBallToInDraft, removeSegment | CHG-012,014,016,017 |
| src/editor/commands.ts | seedDefaultTeams, applyFormations, pruneDanglingBallSegments | CHG-010,012,016 |
| src/editor/uiStore.ts, usePlayback.ts | tour/hasPlayed/playFrom/toast | CHG-011~014 |
| src/ui/timeline/Timeline.tsx(.css) | 배너 한국어, 미해석 블록, 내용 높이 | CHG-014,015,016,017 |
| src/ui/useEditorKeyboard.ts | Space/Tab/모달리티/Ctrl+S 토스트 | CHG-013,014 |
| src/ui/{AutoReactPanel,FormationPicker,DocMenu,HelpPanel,ShortcutsOverlay,Inspector,AppShell,SegmentInspector}.tsx, shell.module.css, i18n/ko.ts | UX/카피 | CHG-010~016 |
| src/editor/qaRound2.test.ts, src/ui/pitch/fling.test.ts, src/ui/tour/tour.test.tsx | 테스트 | — |

## Decisions Made

- 튜토리얼은 "카드 설명" 아닌 "실제 요소 스포트라이트 + 행동 시 자동 진행" (사용자 요청). 쿠키+localStorage 둘 다 기록.
- 기본 팀은 history 밖(seed). 문서 교체는 undo 1회 + 인라인 확인(모달 없음 원칙 유지).
- 패서 = 트랙의 마지막 open-ended 보유자; open-ended possessed 뒤에는 afterSegment 체인 금지(at 트리거).
- 공의 마지막 움직임이 끝난 시각의 드래그 = 새 패스, flick = 킥.
- 보류(설계 결정 유지): Shift+클릭은 스크럽/직선/타원 전용(Ctrl=additive).

## Decision Persistence

- CHANGELOG CHG-010~017, ACTIVE_PLAN R2~R9, CURRENT_STATE 세션 11. UNPERSISTED DECISION: None

## Validation Evidence

- `npm run typecheck && npm run lint && npm test && npm run build && npm run harness:verify && npm run format:check` → PASS (101 tests) — 2026-08-20
- Playwright headless (scratchpad): 첫 방문/투어 8단계/체인 패스/stale/패스 삭제/핸드오버 → PASS, 콘솔 0 에러 (QA 에이전트 6라운드 보고)

## Verification Ownership

- Direct: 위 명령 + headless 스크립트.
- Delegated (사용자): 브라우저 체감 — CURRENT_STATE "라운드 6" 체크리스트(투어, 드래그/플링 감도, 스프링).
- Shared: 없음.

## Failed Attempts

- `:focus-visible`로 마우스/키보드 포커스 구분 → Chromium은 keydown 시점에 이미 true → 입력 모달리티 추적으로 대체.
- `PointerEvent.detail`로 더블클릭 판정 → Chromium pointerdown은 0 → 자체 타이머.
- `endGesture`를 `useCallback([core])`로 메모 → 첫 렌더 상태 고정(stale) → ref 래퍼.
- python 스크립트로 src 파일 덮어쓰기 → vite가 빈 모듈을 캐시(헤드리스 빈 화면) → 변경 파일 `touch`.

## Open Items

- ISSUE-009 리드 패스(달리는 수신자), 타임라인 블록 키보드, 미니바 가장자리 flip, 루즈볼이 번호 가림, 스프링/플링 체감. 46 files uncommitted — 커밋 권장.
