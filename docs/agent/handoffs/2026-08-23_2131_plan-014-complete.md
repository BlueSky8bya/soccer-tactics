# Handoff Snapshot

Created: 2026-08-23 21:31
Agent / Tool: Claude Code (Opus 5)
Task: plan-014-complete
Risk Level: 중간 — 감사에서 시작해 production 수정까지 갔다(pick/B1/compile/relayout-on-restore/blur). 전 게이트 + 브라우저 102 checks로 방어.
Project Version: 0.1.0

## Session Goal

PLAN-014를 전 축 끝까지: 감사(A/B) → 브라우저 감사(D) → 발견된 결함 전부 수정 → UX 핵심(E-core) → 문서(C).
사용자 지시: "계속 이어서 해 끝까지", DG-BROWSER는 "당연히 설치해".

## Completed

- **DG-BROWSER 1안 이행**: Playwright 1.62.1 tracked devDependency, `pw/` runner+manifest+probe 6종.
- **R5(P1) 발견 및 수정**: 호버는 전역 rank(norm), 프레스는 카테고리+intent 우선순위 → 모든 런 끝점
  ±2m에서 약속≠실행. `pressSubject` 단일 판정으로 통합, 진리표 전수 테스트로 계약화.
- **R12-D / R7 Resolved**: 라이브 CTM 실측(7 viewport, 밴드 6~7px, dead strip 0), 취소 경로 전수.
- **P2 6건 수정**: I2 전 waypoint, compile 중복 id 크래시, B1 순간별 예산, 자동저장 복원 relayout,
  수신자 id tie-break, blur 제스처 취소.
- **오진 1건 정정**: F-M1-03(validator dead receiver 미검사)은 사실이 아니었다 — I9/I10 순서 문제.
- **E-core**: 화면-시계 오차 정지/재생중/결과 전부 Δ=0.000m. 초기 0.64m는 측정 왕복 지연이었고
  페이지 내 단일 동기 실행으로 재측정해 정정.
- **C 문서 drift**: 불변식 수, PROJECT_MAP `pw/` 라우팅, Known Issues 재분류, CHANGELOG 185~192.

## Files Touched

| File | Change | Change ID |
|---|---|---|
| src/ui/pitch/pickTarget.ts(+test) | `pressSubject`/`subjectKey` 신설, 진리표 테스트 | CHG-190 / 7d8f4b1 |
| src/ui/pitch/SimplePitch.tsx | 호버가 pressSubject 사용, blur 취소, DEV `hoverKey` 노출 | CHG-189·190·191 |
| src/engine/ballContinuity.ts | 전역 예산 → 순간별 예산 | CHG-191 / e566c9a |
| src/engine/compile.ts | 중복 segment id: 크래시 → issue + skip | CHG-191 |
| src/editor/segmentCommands.ts | 수신자 동률 id tie-break | CHG-191 |
| src/editor/autosave.ts(+test) | 복원 시 relayout | CHG-191 |
| src/editor/tacticFuzz.harness.ts | I2 전 waypoint·handle | CHG-191 |
| src/editor/invariantMutation.test.ts, junctionParity.test.ts | 신규 감사 계약(M1/M2) | CHG-186·187 |
| pw/** | Playwright 인프라 + probe 6종 | CHG-189·192 |
| docs/agent/** | evidence 4건, CURRENT_STATE, PROJECT_MAP, CHANGELOG, plan 아카이브 | CHG-188·192 |

## Decisions Made

- R5 수정 방향: **호버를 프레스에 맞춘다**(반대가 아니라). 프레스 우선순위는 골든 G2 등 기존 계약과
  결합돼 있고, 어긋난 쪽은 나중에 추가된 호버였다.
- `lostpointercapture`는 **취소 신호로 쓰지 않는다** — 정상 종료 시에도 발화하므로 취소하면 모든
  드래그가 깨진다. probe 기대를 앱에 맞춰 정정했다.
- 자동저장 복원 시 relayout 적용(AMB-06 방향 결정): relayout이 이미 "저장소에서 나오는 모든 문서는
  일관되게 도착한다"를 자기 주석으로 약속하고 있었고, 안 하면 첫 편집이 대신 하게 된다.
- B1 예산을 좁히는 것은 detector를 강하게 만드는 방향이라 1800세션 퍼즈로 오탐 부재를 확인한 뒤 채택.

## Decision Persistence

- 전부 CHANGELOG(185~192) + evidence 문서 4건 + CURRENT_STATE에 기록.
- UNPERSISTED DECISION: None.
- ADR 갱신은 하지 않았다 — ADR-0010 D1 2안 비권고는 evidence 문서에 근거와 함께 남겼고,
  Accepted 결정을 뒤집는 변경은 없었다. R5 수정은 기존 계약(골든 G2, intent 우선순위)을 보존한다.

## Validation Evidence

- `npm run typecheck` / `lint` / `build` / `harness:verify` → PASS (warning 0)
- `npm test` → PASS 46 files / **324 tests**
- `node pw/run.cjs` → **102 checks ALL PASS** (hit-scale 42 / gesture-cancel 17 / pick-overlap 13 /
  reduced-motion 5 / r5-diagnose 11 / ux-core 14)
- `ST_FUZZ_SHORT=1500 ST_FUZZ_LONG=300 npx vitest run tacticFuzz` → 1800세션 위반 0
- marathon → **NOT RUN**(소스 미작성)

## Verification Ownership

- Direct: 위 전부 이 세션에서 실행.
- Delegated(사용자): R5 수정 체감, blur 취소 체감, ISSUE-004/007/008 체감.
- Shared: 없음.

## Failed Attempts

- 첫 probe들이 plain 드래그(=토큰 이동)로 저작한 줄 알고 **빈 보드끼리 비교해 헛통과**했다.
  Alt+드래그가 저작 제스처임을 확인하고 비공허성 검사를 넣어 봉인(F-D-02).
- E3 최초 측정 0.64m/1.25m는 앱 결함이 아니라 왕복 지연이었다. 페이지 내 단일 동기 실행으로 정정.
- 셸 heredoc/`node -e`에서 백틱·템플릿 리터럴이 여러 번 깨졌다. 패치 스크립트를 scratchpad 파일로
  쓰고 실행하는 방식이 안전했다.

## Plan Reversals

| ID | New Evidence | Previous Plan | Replacement Plan |
|---|---|---|---|
| F-M1-03 | validator는 dead receiver를 거부한다(직접 호출로 증명) | "validator 보강 필요" | Rejected — I9/I10 발화 순서 문제로 재기술 |
| lostpointercapture | 정상 종료 시에도 발화 | "취소되어야 한다" | probe 기대를 정정, 앱 무변경 |

## Open Questions

- E-polish(contrast/CLS/광범위 viewport)를 할지 — 핵심 감사와 분리된 선택 사항.
- 포인터 마라톤 재작성 여부 — 과거 `pw/marathon.cjs` 소스 없음.

## Known Risks

- production 수정이 5개 파일에 걸쳐 있다(pick/B1/compile/autosave/blur). 전 게이트+브라우저로 방어했으나
  **사용자 체감 확인 전에는 R5·blur 변경의 손맛이 미검증**이다.
- mutation/parity 핀은 detector 지형 특성화라 정당한 리팩토링에도 붉어질 수 있다 — 그때는 재감사 후 재핀.

## Current Working Tree Notes

- uncommitted user changes: 없음 (clean, origin/main과 동기)
- agent changes: 전부 커밋·push 완료. 741c03e 이후 11 커밋.

## Next Exact Step

1. (사용자) `npm run dev` → R5 수정(경로 끝 근처 강조=실제 대상)과 blur 취소 체감.
2. (사용자) ISSUE-004/007/008 체감 판정.
3. (선택) E-polish, 마라톤 재작성.

## Rollback

- R5만 되돌리려면 7d8f4b1 역패치(호버가 다시 `ordered[0]`을 쓴다). 단 `pw/r5-diagnose`가 붉어진다.
- P2 묶음은 e566c9a 역패치. B1 예산만 되돌리려면 `ballSpeedBudget`을 전역 `topBallSpeed`로 환원.
- 브라우저 인프라 제거는 `pw/` 삭제 + `playwright` devDependency 제거(다른 코드 의존 없음).

## Documents Updated

- CURRENT_STATE.md, PROJECT_MAP.md, CHANGELOG_AGENT.md(185~192), ACTIVE_PLAN.md,
  plans/completed/PLAN-20260823-014-…(아카이브), plans/evidence/PLAN-014-{M1,M2,M3,D-browser} 4건

## Documents Possibly Stale

- CODEX_BRIEF.md — 과거 UI 문구가 최신 Accepted와 어긋날 수 있다(감사에서 supersession 추적표는 미작성).
- ADR-0010 D1 — 2안 "조건부 보류" 문구는 유지 중이나, 본 감사 결론은 **비권고**다. 다음에 D1을 건드릴 때
  Amendment로 반영할 것.
