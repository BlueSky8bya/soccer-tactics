# ACTIVE PLAN — PLAN-20260825-017: 전면 캔버스 — 보드가 화면이고, 크롬은 그 위에 뜬다

Plan ID: PLAN-20260825-017
Status: Completed (2026-08-25) — M1~M5 충족(M3는 사용자 피드백 6라운드로 v32→v37 개정, R-4~R-9). 5게이트 PASS(358 tests), 브라우저 216 checks ALL PASS(신규 `pw/full-bleed` 37 포함).
Level: L2 — 셸 레이아웃 구조. 문서 스키마·엔진·도메인·전술 모션 불변.
Trigger: 사용자 2026-08-25 — "애플 형식으로 다 했다고 하는데, 레이아웃 비율 수정을 좀 더 개혁적으로 해봐"
사용자 선택(같은 턴, 옵션 제시 후):
- 비율 = **전면 캔버스 (가장 개혁적)** — 고정 컬럼 0개, 팀·정리는 상단바 팝오버, 재생바는 보드 위에 뜬다.
- 조작법 = **맥락 힌트만 보드 위에** — 상시 패널 없음, 전체 목록은 `?` 오버레이.

## 진단 — 실측 (1440×900 / 1280×800, light, dev)

| 항목 | 1440×900 | 1280×800 |
| --- | --- | --- |
| 고정 사이드 크롬 | 222 + 238 = **460px (32%)** | 460px (**36%**) |
| `<main>` | 980×753 | 820×653 |
| 보드 프레임(svg) | 956×729 | 796×629 |
| **그려진 피치(105×68 마킹)** | **921×596** | 767×497 |
| 하단 밴드 | 폭 1440 중 알약 845 → **좌우 595px 빈 띠** | 1280 중 820 |

그 460px 안에 든 카드 6장 중 **5장이 정적 설명문**(Ctrl 단축키 / 재생 / 배치 / 경로 그리기 — 그리고 이 내용은 `?` 오버레이에 전부 다시 있다). 즉 화면의 3분의 1을 **중복된 치트시트**가 상시 점유한다. 이것이 "비율이 아직 애플이 아니다"의 실체다 — 색·스프링은 애플이지만 골격은 2010년대 IDE(좌 도구/우 인스펙터/하단 도킹 바)다.

## Decision (ADR-0009 v31로 승격)

1. **셸은 두 행뿐**: `상단바(48) / 캔버스(1fr)`. 좌·우 고정 컬럼 삭제.
2. **보드는 창을 채운다**(full-bleed). `usePitchView`가 이미 요소 aspect로 viewBox를 키우므로 늘어난 면적은 전부 좌표 있는 잔디가 된다.
3. **재생바는 보드 위에 뜬다** — `<footer>`가 격자 행을 먹지 않고 캔버스 하단 중앙에 absolute.
4. **피치는 안전 영역에 앉는다**: 마킹 스케일은 `min(w/baseW, (h − safeBottom)/baseH)`. 바가 잔디를 덮되 **경기장 마킹을 가리지 않는다**. 잔디(surround)는 여전히 창 끝까지 — 펜·포인터 좌표 손실 없음.
5. **팀 구성 / 정리·설정은 상단바 팝오버**(`팀 ▾`, `보드 ▾`). 버튼과 단축키 표기는 그대로 옮긴다.
6. **상시 조작법 패널 삭제**. 대신 `BoardHints` — `useActiveCues()`(Ctrl/Alt/Shift/Space/공/선수/경로 게이트, 이미 존재)가 켠 상태에 해당하는 줄만 보드 좌상단에 최대 3줄. 아무 상태도 아니면 **아무것도 없다**.
7. **F(zen)의 뜻이 바뀐다**: "패널 접기" → "떠 있는 크롬 감추기"(바·힌트·단계 패널). 패널이 없어졌으므로 옛 뜻은 성립하지 않는다.

## Verifiable End State

- **M1 골격** — `.shell[data-simple]`이 1열 2행. `sideLeft`/`sideRight` 규칙과 컴포넌트 소멸. 재생바는 캔버스 위 absolute, 재생 중 recede 유지.
- **M2 상단바 메뉴** — `양 팀 채우기 / 움직임 전체 지우기 / 새로 시작 / 공 휙 던지기`가 전부 접근 가능(role=button, 이름 동일). 키보드로 열고 닫힌다 — Enter가 열고, Esc가 닫으며 포커스는 트리거로 돌아온다(실측). **Space는 열지 않는다** — 앱 전역에서 Space는 재생이고 `useEditorKeyboard`가 먼저 먹는다. 이 앱의 기존 계약이라 메뉴가 뒤집지 않는다. 바깥 클릭과 Tab이 카드를 벗어나는 것도 닫는다. (화살표 로빙은 없다: 이 카드는 메뉴가 아니라 컨트롤 묶음이다 — `MenuButton` 주석 참조.)
- **M3 왼쪽 여백의 키 가이드**(v33으로 재개정) — 유휴 상태에서 200px 열(만들기/보기/정리 3그룹, 행마다 `[캡] 낱말`)이 서 있고 **열린 행 0 · 강조 0**. 키를 실제로 쥐면 그 행만 열리며 강조되고(ExposeHK 리허설), 호버·포커스로도 열리며, 클릭은 고정한다. 상세는 **열 안에서만** 열려 피치를 덮지 않는다. 열의 오른쪽 끝 ≤ 마킹의 왼쪽 끝(3해상도 실측). `?` 오버레이는 전체 목록을 계속 들고 있고, 열 아래 `? 단축키 전체 보기`가 그리로 연결한다.
  · 두 번 개정됐다: R-4(맥락에만 숨김 → 상시 색인), R-5(상단 레일 → 왼쪽 여백의 열). 조사 근거는 `docs/product/DISCOVERABILITY_RESEARCH_2026-08-25.md`.
- **M4 비율(실측으로 증명)** — 그려진 피치 폭이 1440에서 **921 → 1102**, 1280에서 **767 → 957**, 1024에서 **~540 → 910**. 바가 피치 마킹을 덮지 않음(3해상도 픽셀 실측).
  · 착지 수정: 초안 목표(1440에서 1130)는 **바 간섭을 무시한 값**이었다. 마킹이 바 위에서 끝나야 한다는 §4 계약을 지키면 900px 높이 창의 상한은 1102다. 목표를 낮춘 것이 아니라 **계약을 우선**했다(zen에서는 1208).
- **M5 게이트** — `typecheck && lint && test && build && harness:verify` PASS, `pw` 프로브 전부 PASS(203), 두 테마 스크린샷 확인. 레이아웃 계약은 **상시 프로브 `pw/full-bleed`**로 고정한다 — 문서가 아니라 픽셀이 지킨다.

## Non-Goals

- 엔진·도메인·문서 스키마·전술 모션 불변.
- 도킹 표면에 backdrop-filter 금지(A-04a) — 이제 도킹 표면은 상단바뿐이고, 여전히 불투명.
- 고채도 다색 버튼 금지(VDR-0001 배제 3).
- 튜토리얼 흐름(5장) 유지. 대상만 새 상단바 버튼으로 옮긴다.

## Execution Order

1. **M1** 셸 격자 + 떠 있는 재생바
2. **M2** 상단바 팝오버(팀 / 보드)
3. **M3** BoardHints + GuidePanel 삭제
4. **M4** safeBottom 피치 배치 + 실측
5. **M5** 테스트·프로브·문서(ADR-0009 v31, CURRENT_STATE, CHANGELOG_AGENT)

## Ambiguity Register

| # | 모호했던 것 | 어떻게 닫았나 |
| - | --- | --- |
| A-1 | "개혁적"이 어디까지인가 | 실측 3안(전면 캔버스 / 인스펙터 1개 / 레일+인스펙터)을 수치와 함께 제시 → 사용자가 **전면 캔버스** 선택. |
| A-2 | 상시 조작법을 어디로 | 3안 제시 → 사용자가 **맥락 힌트만** 선택. `?` 오버레이가 전체 참조를 계속 보유. |
| A-3 | 떠 있는 바가 선수를 가리는가 | 가릴 수 있다. 그래서 피치 마킹 스케일에 `safeBottom`을 도입해 **마킹은 바 위에서 끝나고** 잔디만 바 밑으로 이어진다. |
| A-4 | PLAN-016 M4("보드 점유율 현재 이상")와 충돌하는가 | 충돌 아님 — 같은 방향의 강화. 016은 컬럼을 유지한 채 점유율을 지켰고, 017은 컬럼을 없앤다. 016의 "패널 폭 하한 222px"는 패널이 사라지므로 **무효**(v31에 명시). |

## Plan Reversal Log

| # | 되돌린 것 | 이유 |
| - | --- | --- |
| R-1 | `BOARD_SAFE_BOTTOM_PX = 96`(바 높이 전체) → 72 | 유도해 보니 96은 아무도 요구하지 않은 여백에 스케일 25px을 버렸다. 바 밑에 있어야 할 것은 피치의 2m 서라운드다. |
| R-2 | `MenuButton`의 render-prop(`children(close)`) → `data-menu-keep` 클릭 규칙 | 렌더 중에 함수 prop을 호출하면 `react(refs)` 린트가 잡는다(ref가 렌더에서 도달 가능). 규칙을 DOM에 두니 "명령은 닫고 설정은 열어 둔다"가 코드가 아니라 **마크업이 말하는 사실**이 됐다. |
| R-9 | **열 폭을 상수로 둔 것**(136/62) → 슬랙의 몫 (ADR-0009 **v37**) | 사용자 2026-08-25: "너무 너비가 짧아서 보기 힘들어." 슬랙이 263~1119px로 창마다 다른데 상수 하나로 맞을 리가 없었다 — 1905×858에서 800px 중 136px만 쓰고 문장은 어절 중간에서 접혔다. 폭을 몫(0.72/0.28)으로 두고 하한 136/52·상한 288/122로 물렸고, 남는 몫의 절반만큼 열이 보드 쪽으로 붙는다. |
| R-8 | **모든 안내를 왼쪽 한 열에 쌓은 것**(v33~v35) → 두 여백에 분배 (ADR-0009 **v36**) | 사용자 2026-08-25: "확장되다보면 세로로 넘어버리잖아. 오른쪽으로 좀 분배하든지 해." 실측이 같았다 — Space를 펼치면 열이 857px, 보드는 728(1280×800)이라 **129px가 화면 밖**이었다. 가로 슬랙은 263~479px라 두 열이 들어간다: 키 136(예약 168) + 명령 62(예약 90) = 258 ≤ 263. 보드 점유율은 그대로. |
| R-7 | **정리·설정을 팝오버에 둔 것**(v31) → 사이드 열의 카드 (ADR-0009 **v35**) | 사용자 2026-08-25: "이 박스도 사이드로 옮겨줘." v31의 판단은 **당시 전제** 위에서 맞았다 — 보드에 내줄 공간이 없었다. v33이 왼쪽 여백(피치가 못 쓰는 잔디)을 열었으므로 전제가 바뀌었고, 카드 하나의 비용은 0px이다. 함께: 접힘 조건을 창 폭 → **여백(가로세로비 1.57)** 으로 바꿨다 — `pw/hit-scale`이 1440×1000에서 열이 터치라인을 덮는 것을 잡았다. |
| R-6 | **호버로 서랍을 여는 것**(v33) → 호버는 색만 (ADR-0009 **v34**) | 사용자 2026-08-25: "호버링 했을 때 움직임이 너무 많아서 어지럽고." 포인터가 열을 스치기만 해도 서랍이 연달아 열려 아래 행 전부가 밀렸다. 근거(ExposeHK)가 말한 것은 **키를 쥐면 연다**이지 포인터가 지나가면 연다가 아니다. 기하는 클릭·포커스·키 유지에서만 움직인다. 함께: 패딩을 동심 규칙으로 다시 잡고(카드 22 − 12 = 행 10), 두 열은 슬랙을 넘겨 보드를 14% 깎으므로 열은 하나로 두고 행을 9 → 7로 줄였다(`X`·`⇧R`는 `보드 ▾` 버튼이 이미 캡을 달고 있다). |
| R-5 | **상단 한 줄 레일**(v32) → **왼쪽 여백의 열**(v33) | 사용자 2026-08-25: "왜 위쪽에 나열했어 좌/우 남는 여백이 이렇게 많은데." 맞다 — 보드는 전 구간 **높이 제약**이라 1440×900에서 한쪽 135px의 잔디가 영원히 남는다. 그리고 낱말 하나(`Ctrl 선수`)로는 직관이 서지 않았고, 펼친 상세가 피치를 덮었다. 조사를 다시 해서(ExposeHK / KeyMap / CommandMaps) 자리·크기·펼침 규칙을 근거 있는 값으로 다시 놓았다. 예약은 **슬랙 상한**이라 보드 점유율은 그대로다(실측 동일). |
| R-4 | **힌트를 전부 맥락으로 숨긴 것**(M3 원안) → 상시 키 레일 + 맥락 확장 (ADR-0009 **v32**) | 사용자 2026-08-25: "단축키가 아예 다 사라져서 처음 들어오는 사람은 뭘 어떻게 해야 할지 모를 것 같은데." 맞다 — 맥락 힌트는 **이미 아는 사람에게만 보인다**. 처음 온 사람은 Ctrl을 누를 이유가 없으니 Ctrl이 선수를 놓는 키라는 걸 배울 길이 없었다. 컬럼을 되돌린 것이 아니라(두 개 × 전체 높이 → 한 줄 27px), 색인과 설명을 **다른 층으로 갈랐다**. |
| R-3 | 새 `.menu` 클래스 | 같은 모듈에 죽은 `.menu`(260px 드롭다운 잔재)가 있어 **같은 해시로 병합**돼 툴바 버튼이 260px이 됐다(실측). 죽은 규칙을 지웠다. |
