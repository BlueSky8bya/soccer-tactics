# presets/

Data-driven presets only (formations, scenario templates). No React. Applied by editor commands; never a constraint on entity positions.
Arrives in M1 (formations.json) and M3 (scenarios/).
